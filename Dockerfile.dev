FROM node:20-slim

WORKDIR /app

# Enable corepack to use the built-in pnpm
RUN corepack enable

COPY package.json pnpm-lock.yaml* ./

RUN pnpm install

COPY . .

EXPOSE 8000

CMD ["pnpm", "run", "dev"]
